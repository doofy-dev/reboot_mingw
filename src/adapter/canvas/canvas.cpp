#include <adapter/canvas/canvas.h>

namespace reboot_adapter
{
	Canvas::Canvas():m_Width(100), m_Height(100),m_Title((char *)"Reboot engine")
	{
		
	}
	bool Canvas::init(int width, int height, char* title)
	{
		return false;
	}
}